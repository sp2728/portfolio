import { SlideFromLeftDirective } from './slide-from-left.directive';

describe('SlideFromLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideFromLeftDirective();
    expect(directive).toBeTruthy();
  });
});
